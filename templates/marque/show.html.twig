{% extends 'base.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>Nom de la Marque</th>
                <td>{{ marque.nomMarque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_marque_index') }}">Retour à la liste</a>

    <a href="{{ path('app_marque_edit', {'id': marque.id}) }}">Éditer</a>

    {{ include('marque/_delete_form.html.twig') }}
{% endblock %}
