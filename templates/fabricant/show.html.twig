{% extends 'base.html.twig' %}

{% block title %}Fabricant{% endblock %}

{% block body %}
    <h1>Fabricant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fabricant.id }}</td>
            </tr>
            <tr>
                <th>Nom du Fabricant</th>
                <td>{{ fabricant.nomFabricant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fabricant_index') }}">Retour à la liste</a>

    <a href="{{ path('app_fabricant_edit', {'id': fabricant.id}) }}">Éditer</a>

    {{ include('fabricant/_delete_form.html.twig') }}
{% endblock %}
